[package]
name = "fixture_hook"
version = "0.1.0"
edition = "2021"
description = "Trivial Token-2022 transfer hook for the hook-switched-on issuer scenario"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pinocchio = "=0.9.2"

[lints.rust]
unexpected_cfgs = { level = "allow" }
